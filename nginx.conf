server {
    listen 80;
    server_name direct-line-sar.ru www.direct-line-sar.ru;

    location = /favicon.ico { access_log off; log_not_found off; }
    
    location /static/ {
        alias /var/www/directsite/directsite/staticfiles/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/var/www/directsite/directsite.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # HTTPS редирект (после настройки SSL)
    # return 301 https://$server_name$request_uri;
}

# HTTPS сервер (после настройки SSL)
# server {
#     listen 443 ssl http2;
#     server_name direct-line-sar.ru www.direct-line-sar.ru;
#     
#     ssl_certificate /etc/letsencrypt/live/direct-line-sar.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/direct-line-sar.ru/privkey.pem;
#     
#     location /static/ {
#         alias /var/www/directsite/directsite/staticfiles/;
#     }
#     
#     location / {
#         include proxy_params;
#         proxy_pass http://unix:/var/www/directsite/directsite.sock;
#     }
# }
